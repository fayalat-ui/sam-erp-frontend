import WorkerDetail from "@/modules/trabajadores/WorkerDetail";

/**
 * Página wrapper del detalle de trabajador.
 * Esta página existe solo para el router.
 * El contenido real vive en el módulo.
 */
export default function TrabajadorDetalle() {
  return <WorkerDetail />;
}
